{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "dependencies": {
    "firebase-admin": "~5.5.0",
    "firebase-functions": "^0.7.5",
    "kotlin": "^1.2.30"
  },
  "scripts": {
    "start": "firebase serve --only functions:saveString",
    "deploy": "../gradlew build && firebase deploy --only functions"
  },
  "private": true
}
